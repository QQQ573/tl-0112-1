version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    depends_on:
      - image-proxy
    environment:
      VITE_PROXY_URL: http://localhost:3001/proxy
    restart: unless-stopped

  image-proxy:
    build:
      context: ./proxy
      dockerfile: Dockerfile
    ports:
      - "3001:8080"
    environment:
      - NODE_ENV=production
      - PORT=8080
    restart: unless-stopped
